import React, { useEffect, useState, useContext } from "react"

const HomePage = ({ }) => {
    useEffect(() => {

    }, [])
    return (
        <>
            <h1 className="text-[120px]">Billhhshshs</h1>
        </>
    )
};

export default HomePage;
